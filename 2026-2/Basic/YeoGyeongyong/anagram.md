# 애너그램 변환 최소 삭제

## 첫 접근

두 문자열을 이중 반복하며 같은 문자를 1:1 매칭

이미 매칭된 문자는 다시 사용하지 않도록 처리

(시간 복잡도가 비효율적으로 느껴져 폐기)

## 풀이 과정

두 문자열의 각 문자에서 'a'를 빼 0~25 인덱스로 변환해 개수를 저장

두 배열의 값의 차에 절대값을 씌워 합산

## 실수

- 절대값 누락
