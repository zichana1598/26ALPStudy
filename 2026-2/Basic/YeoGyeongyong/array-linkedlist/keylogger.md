# keylogger

## 풀이 과정

테스트 케이스 별로 문자열 처리

연결 리스트 사용해서 커서 동작 구현

모든 처리 후 리스트 순회하며 최종 출력

## 실수

- if문을 반복적으로 사용하여 커서 처리 조건을 구별하려 했지만 배타적 처리를 안해 커서 처리 문자가 list에 들어감
- 백스페이스 처리 시 커서가 맨 앞에 있는 경우 고려 안함
- 문자 하나 처리할 때마다 출력되게 출력문 위치를 잘못 잡음
